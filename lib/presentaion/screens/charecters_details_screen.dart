import 'package:flutter/material.dart';

class CharectersDetailsScreen extends StatelessWidget {
  const CharectersDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}